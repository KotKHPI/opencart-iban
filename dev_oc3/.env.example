## Copy to dev_oc3/.env and run docker compose with: --env-file dev_oc3/.env
##
## Example:
##   docker compose --env-file dev_oc3/.env -f dev_oc3/docker-compose.yml up -d --build
##   docker compose --env-file dev_oc3/.env -f dev_oc3/docker-compose.yml -f dev_oc3/docker-compose.dev.yml up -d --build

# OpenCart version (git tag from https://github.com/opencart/opencart)
OPENCART_VERSION=3.0.3.8

# HTTP base URL that OpenCart writes into config.php (must end with /)
OPENCART_PORT=8081
OPENCART_HTTP_SERVER=http://localhost:8081/

# Admin user created by installer
OPENCART_USERNAME=admin
OPENCART_PASSWORD=admin
OPENCART_ADMIN_EMAIL=admin@example.com

# Install language (must exist in the OpenCart distribution)
OPENCART_LANGUAGE=en-gb

# Database
DB_DRIVER=mysqli
DB_HOSTNAME=db
DB_PORT=3306
DB_DATABASE=opencart
DB_USERNAME=opencart
DB_PASSWORD=opencart
DB_PREFIX=oc_

# Xdebug (optional)
# Enable by setting XDEBUG_MODE=debug,develop and configuring your IDE to listen on port 9003.
XDEBUG_MODE=off
XDEBUG_CLIENT_HOST=host.docker.internal
XDEBUG_CLIENT_PORT=9003
PHP_IDE_SERVER_NAME=opencart_oc3
